events {}

http {
    server {
        listen 8080;

        location / {
            include /etc/nginx/snippets/mnemonic-dashboard-api-policy.conf;
            proxy_set_header Host $host;
            proxy_pass http://127.0.0.1:3000;
        }
    }
}
